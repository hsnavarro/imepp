#include <bits/stdc++.h>
using namespace std;

const int N = 2e5 + 5, K = 1e2 + 5;

int n, k;
int a[N];
int dp[N][K];
int c[N][K];

int main() {
  ios_base::sync_with_stdio(0), cin.tie(0);
  cin >> n >> k;
  for(int i = 1; i <= n; i++) cin >> a[i];
  
  if(!a[1]) {
    for(int i = 1; i <= k; i++) dp[1][k] = 
  }


}
