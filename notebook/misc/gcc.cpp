// Count one's in binary representation
// __builtin_popcount(x)

// Check parity (parity of number of one's in binary representation) 
// 1 - odd, 0 - even
// __builtin_parity(x)

// Count leading zeros (number of zeros before the first occurrence of one)
// __builtin_clz(x)

// Count trailing zeros (number of zeros from last to first ocurrence of one)
// __builtin_ctz(x)

// For long funcNamel(x)
// For long long funcNamell(x)
